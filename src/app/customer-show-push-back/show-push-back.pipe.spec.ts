import { ShowPushBackPipe } from './show-push-back.pipe';

describe('ShowPushBackPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowPushBackPipe();
    expect(pipe).toBeTruthy();
  });
});
